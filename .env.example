# scitex-web environment variables
#
# `cp .env.example .env` and edit. Loaded automatically when the package
# initializes; explicit kwargs/CLI flags still take precedence.

# SCITEX_WEB_DOWNLOADS_DIR=
